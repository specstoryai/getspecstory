# SpecStory explanation file
.specstory/.what-is-this.md
.idea/

dist/
# deadreckon run + DoD artifacts (root-anchored). NOTE: .specstory is tracked here,
# same as dev - do not blanket-ignore it.
/.deadreckon/

# stray deadreckon run artifact written at repo root
deadreckon-plan-consolidated.json

# local .specstory artifacts not tracked on dev (history itself stays tracked)
.specstory/statistics.json
.specstory/cli/
